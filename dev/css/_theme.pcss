/**
 * theme
 */

/* loadingbar */
.loadingbar {
  background-color: #ffa500;

  dt, dd {
    box-shadow: #ff0000 1px 0 6px 1px;
  }
}

/*  header */
.nfm.header {
  background-color: #191919;
  border-bottom: 2px solid #444;
}

.nfm.header .nfm.menu {
  .item {
    color: rgba(255, 255, 255, .7);
  }

  .nfm.logo {
    color: rgba(255, 255, 255, .9);
  }

  a.item:hover {
    color: rgba(255, 255, 255, .9);
  }
}

/* sidebar */
.nfm.sidebar {
  background-color: rgba(25, 25, 25, .95);
  border-right: 1px solid rgba(34, 36, 38, .15);
}

.nfm .main-sidebar li > a {
  color: rgba(255, 255, 255, .6);

  &:hover {
    color: rgba(255, 255, 255, .9);
  }
}

/* content */
.nfm.content {
  background-color: rgba(255, 255, 255, .7);
  border-left: 5px solid #444;
}

/* operate */
.nfm.operate {
  .nfm.buttons .button:hover {
    color: #000;
    background-color: rgba(255, 255, 255, .9);
  }

  .nfm.buttons .button:active,
  .nfm.buttons .button:focus {
    color: rgba(255, 255, 255, .9);
    background-color: #0B8BF1;
  }

  .nfm.buttons .detele.button:hover,
  .nfm.buttons .detele.button:focus,
  .nfm.buttons .detele.button:active {
    color: rgba(255, 255, 255, .9);
    background-color: #DB2828;
  }
}

/* table */
.nfm.table {
  tbody tr:nth-child(even) {
    background-color: rgba(0, 0, 50, .02);
  }

  tbody tr:hover, tbody tr:active {
    box-shadow: 0 0 0 #C9BA9B inset;
    background-color: #fffaf3;

    td {
      color: #573a08;
    }
  }

  a {
    color: rgba(0, 0, 0, .87);
  }

  .ui.input input:focus {
    border-color: rgba(0, 0, 0, .3);
  }
}

/* 自定义icon颜色 */
i.i-color.icon {
  color: rgba(61, 163, 239, .95);
}