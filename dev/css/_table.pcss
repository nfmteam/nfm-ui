/* 自定义table样式 */
.nfm.table {
    .ui.checkbox label {
        padding-left: 0;
    }
    tbody {
        tr:nth-child(even) {
            background-color: rgba(0, 0, 50, .02);
        }
        td {
            font-size: 13px;
        }
    }
}